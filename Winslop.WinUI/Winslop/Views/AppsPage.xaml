<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winslop.Views.AppsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winslop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Display mode selector (replaces my WInforms comboDisplayMode) -->
        <ComboBox x:Name="comboDisplayMode" Grid.Row="0" HorizontalAlignment="Stretch"
              x:Uid="ComboDisplayMode" FontSize="12" Height="30"
              Margin="0,0,0,8" />

        <!-- App list with checkboxes (replaces CheckedListBox) -->
        <Border Grid.Row="1"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="0" CornerRadius="4" Padding="4">
            <ListView x:Name="listApps" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:AppItem">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{x:Bind IsChecked, Mode=TwoWay}"
                                      MinWidth="0" MinHeight="0" Padding="4,0,0,0" />
                            <TextBlock Text="{x:Bind DisplayName}" VerticalAlignment="Center" FontSize="11" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>